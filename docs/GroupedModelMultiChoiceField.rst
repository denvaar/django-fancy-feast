GroupedModelMultiChoiceField
=======================

``GroupedModelMultiChoiceField`` is just like the ``GroupedModelChoiceFeild``, but allows multiple selections. Refer to [these docs]() for
field options and examples.
